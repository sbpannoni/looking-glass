# secrets & local state — never commit
.env
*.env
server/config/server.yaml
server/certs/
server/logs/
server/hud/audio/
server/hud/looking-glass.cer

# worker local files
worker/run-stt.bat
worker/stt_server.log

# python
__pycache__/
*.pyc
.venv/
venv/

# os
.DS_Store
Thumbs.db
nohup.out

# self-signed cert exported for per-device browser trust (host-specific)
server/hud/jarvis.cer

# Piper TTS voices (296MB of .onnx) — downloaded at setup, never committed
server/models/

# editor/session backup snapshots
*.bak-*

# generated by claude-control's coder_models_refresh.py -- refreshed data,
# not source; tracking it just churns the diff on every refresh run
server/config/coder_model_metrics.json
server/config/coder_orchestrator_metrics.json
server/config/coder_review_metrics.json
