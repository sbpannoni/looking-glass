# Texture credits

None currently. The network map's terrain is procedural (see the TRON grid
landscape section of `hud/network-map.js`) and uses no image assets — a
photographic lunar surface was tried and removed, because a single texture
stretched across thousands of world units is magnified far too much to ever
look sharp.
